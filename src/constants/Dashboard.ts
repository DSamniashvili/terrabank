import { config } from 'utils/config';

export const OPEN_CARD_WIDTH = config.mobileWidth - 48 - 24;
export const OPEN_CARD_HEIGHT = 200;
export const CLOSE_CARD_WIDTH = 200;
export const CLOSE_CARD_HEIGHT = 120;
