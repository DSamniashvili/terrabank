import { Text, View } from 'react-native';
import React from 'react';
import { config } from 'utils/config';

const OtherBanksProducsts = () => {
  return (
    <View style={{ width: config.mobileWidth }}>
      <Text>OtherBanksProducsts</Text>
    </View>
  );
};

export default OtherBanksProducsts;
