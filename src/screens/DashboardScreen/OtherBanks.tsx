import { Text, View } from 'react-native';
import React from 'react';
import { config } from 'utils/config';

const OtherBanks = () => {
  return (
    <View style={{ width: config.mobileWidth, marginTop: 30, padding: 24 }}>
      <Text>OtherBanks</Text>
    </View>
  );
};

export default OtherBanks;
