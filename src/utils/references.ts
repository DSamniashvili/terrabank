import { References } from 'types/declarations/references';

const references: References = {
  dropdownAlert: null,
  reduxStore: null,
  navigator: null,
  loader: null,
  modal: null,
  bottomModal: null,
  reduxDispatch: null,
};

export default references;
